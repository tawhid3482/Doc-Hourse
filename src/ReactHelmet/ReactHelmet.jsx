import { Helmet } from 'react-helmet-async';

const ReactHelmet = ({title}) => {
    return (
        <div>
            <Helmet> 
                
            </Helmet>
            <title>{title}</title>
        </div>
    );
};

export default ReactHelmet;